import React from 'react';
import './NavBar.css'


const NavBar = (props) => (
  <nav className="NavBar">
    <a href="#">New Search </a>
    <a href="#" className="user">Log In </a>
  </nav>
);

export default NavBar;
